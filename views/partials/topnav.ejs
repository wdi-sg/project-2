<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
    <% if (currentUser) { %>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbar-top" aria-controls="navbar-top" aria-expanded="false" aria-label="Toggle navigation">
      <i class="fa fa-bars" aria-hidden="true"></i>
    </button>
    <h2 class="navbar-brand mb-0">Homework Manager</h2>
    <% if (currentUser.role === "teacher") { %>
    <div class="collapse navbar-collapse" id="navbar-top">
        <ul class="navbar-nav mr-0">
          <li class="nav-item"><a class="nav-link" href="/profile/<%= currentUser._id %>/dashboard">Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile/<%= currentUser._id %>">Profile</a></li>
          <li><a class="nav-link" href="/classroom">Add/Join Classes </a></li>
          <li class="nav-item"><a class="nav-link" href="/assignment/create">Add Assignments </a></li>
        </ul>
        <input class="btn btn-outline-success my-0 mx-0" type="submit" onclick="location.href='/auth/logout'" value="Log Out">
      </div>
    <% } else if (currentUser.role === "student") {  %>
      <div class="collapse navbar-collapse" id="navbar-top">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active"><a class="nav-link" href="/profile/<%= currentUser._id %>/dashboard">Dashboard</a></li>
          <li></li>
        </ul>
        <input class="btn btn-outline-success" type="submit" onclick="location.href='/auth/logout'" value="Log Out">
      </div>
    <% } %>
  <%  } else {%>
  <h2 class="navbar-brand mb-0">Homework Manager</h2>
  <% } %>
</nav>
