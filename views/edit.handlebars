<form action="/edit/{{announcements._id}}" method="post">

  <div>
    <label>Announcement: </label>
    <textarea class="form-control" name="content">{{announcements.content}}</textarea>
  </div>

  <div>
    <br>
    <input type="submit" class="btn btn-success">
  </div>
</form>
