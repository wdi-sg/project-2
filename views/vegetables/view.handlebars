<div class="page-header">
  <div class="page-header-image" data-parallax="true" style="background-image: url('/assets/img/wallpaper3.jpg');"></div>
  <br><br><br><br>
  <div class="container">
    <div class="row">
      {{#each supplier.vegetables as |vegetable| }}
        <div class="col-sm-4 ">
          <div class="text-center">
            <div class="card card-plain">
              <img src="{{ vegetable.photo }}"><br>
              <p class="card-text">Name : {{vegetable.name}}</p>
              <p class="card-text">Price per KG $ : {{ vegetable.price }}</p>
              <p class="card-text"> Minimum Quantity in KG : {{ vegetable.quantity}}</p>
            </div>
          </div>
        </div>
{{/each }}
</div>
</div>
</div>


{{!-- <h3>List of Vegetables</h3>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Vegetable Name</th>
        <th scope="col">Price per KG $</th>
        <th scope="col">Minimum Quantity in KG</th>
        <th scope="col">Photo</th>

        <th scope="col"></th>
      </tr>
    </thead>
    {{#each supplier.vegetables as |vegetable|}}

    <tbody>
      <tr>
        <td>{{ vegetable.txtName }}</td>
        <td>{{ vegetable.txtPrice }}</td>
        <td>{{ vegetable.txtQuantity }}</td>
        <td><img src="{{ vegetable.photo }}"></td>


        <td>

          <form class="" action="/vegetable/new" method="post">
            <input type="hidden" name="id" value="{{supp.id}}">
            <button type="submit" name="button" href="/vegetable/new">Edit</button>
          </form>
        </td>

      </tr>
    </tbody>

{{/each}}
</table> --}}
{{!-- <a class="btn btn-primary" href="/components/new" role="button">Add</a> --}}
