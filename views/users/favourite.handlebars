<h2>Hey there {{ user.username }}, </h2>

<div>
  <p>These are your favourite carparks: </p>
  <ul multiple>
  {{#each user.carparks as |carpark|}}
    <li>
      <h3>{{ carpark.name }}</h3>
      <p>{{ carpark.address }}</p>
      <p>{{ carpark.id }}
      <form action="/{{ user.username }}?_method=delete" method="post">
        <button>Delete</button>
      </form>
    </li>
  {{else}}
    <li>no place</li>
  {{/each}}
</ul>
</div>
