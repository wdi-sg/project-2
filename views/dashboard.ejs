<div class="container">


<h2>Hi, <%= currentUser.name %></h2>



<% if (currentUser.role === "teacher") { %>
  <div class="col-md-10">
    <div class="row">
      <div class="col-md-10">
        <div class="row">
          <div class="col-md-12">
            <div class="content-box-header">
              <div class="panel-title">Your Assignments: </div>
            </div>
            <div class="content-box-large box-with-header">
              <div class="table-responsive">
                <table class="table">
                      <thead>
                        <tr>
                          <th></th>
                          <th>Title</th>
                          <th>Description</th>
                          <th>Due Date</th>
                          <th>Est Time to Complete</th>
                          <th>Class</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% assignments.forEach(function (elem) { %>
                        <tr>
                          <form method="POST">
                          <td><input type="checkbox" name="id" value="<%= elem._id %>"> </td>
                          <td> <%= elem.title %> </td>
                          <td><%= elem.description %> </td>
                          <td><%= elem.due_date %></td>
                          <td><%= elem.est_time %>hrs</td>
                          <td><%= elem.classroom %></td>
                        </tr>
                          <% }) %>
                        <tr>
                          <td colspan="6">
                            <input type="submit" formaction="" class="btn btn-outline-success" value="Edit"></a>
                            <input type="submit" formaction="/assignment?_method=DELETE" class="btn btn-outline-danger" value="Delete">
                        </td>
                        </tr>
                      </tbody>
                      </form>
                    </table>


              </div>
            <br /><br />
          </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
  <% } else if (currentUser.role === "student") {  %>
<!--
    <div class="page-content">
      <div class="row">
      <div class="col-md-2">
        <div class="sidebar content-box" style="display: block;">
                <ul class="nav">
                  <li class="current"><a href="index"> Dashboard</a></li>
                    <li><a href="/profile/view/:id"> Profile </a></li>
                    <li><a href="/auth/logout">Log Out</a></li>
             </div>
      </div> -->

      <div class="col-md-10">
        <div class="row">
          <div class="col-md-6">
            <div class="content-box-large">
              <div class="panel-heading">
              <% if (user.classrooms === null) { %>
              <div class="panel-title">Please join a class: </div>
              <% } else {%>
                <div class="panel-title">Please join a class: </div>
              <% } %>
              </div>
              <div class="panel-body">

              </div>

    <% } %>

</div>
